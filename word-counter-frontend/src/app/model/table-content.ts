export class TableContent {
  position: number;
  word: string;
  count: string;

  constructor(position: number, word: string, count: string) {
    this.position = position;
    this.word = word;
    this.count = count;
  }
}
